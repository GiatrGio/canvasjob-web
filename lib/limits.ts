export const FREE_TRACKED_JOB_LIMIT = 5;
export const FALLBACK_EVALUATION_LIMIT = 50;
// Fallback when a profile row lacks monthly_cover_letter_limit. Matches the
// free-tier DB default in backend migration 0015.
export const FALLBACK_COVER_LETTER_LIMIT = 1;
