"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"
file_include_globs = ".env.local\n"

[scripts]
setup = "npm ci"
run = "(test -x node_modules/.bin/next || npm ci) && npm run dev -- --hostname 127.0.0.1 --port ${CONDUCTOR_PORT:-3000}"
run_mode = "concurrent"
